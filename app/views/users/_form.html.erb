<%= form_with model: user do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>First Name : <%= form.text_field :first_name %></div>
  <div>Last Name : <%= form.text_field :last_name %></div>
  <div>Email : <%= form.text_field :email %></div>
  <br>
  ===================== Agent =================================
  <%= form.fields_for :agent do |agent_form| %>
    <div>Title : <%= agent_form.text_field :title %></div>
    <div>managment : <%= agent_form.text_field :managment %></div>
      <div><% form.hidden_field :id %></div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
